version: '3'
services:
  nacos-server:
    image: nacos/nacos-server:v2.4.3
    container_name: nacos-server
    network_mode: "host"
    environment:
      - MODE=standalone
      - SPRING_DATASOURCE_PLATFORM=derby
    restart: always